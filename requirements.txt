# ============================================================
# MedGuard AI — requirements.txt
# Compatible: Python 3.11 / 3.12 / 3.13  |  Windows / Mac / Linux
#
# INSTALL ORDER (run these 3 commands in sequence):
#   pip install -r requirements-core.txt
#   pip install -r requirements-ml.txt
#   pip install -r requirements-ai.txt
#
# OR install everything at once (may take 10-15 min):
#   pip install -r requirements.txt
# ============================================================

# ── Core API ────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
httpx>=0.27.0
pydantic>=2.7.1
pydantic-settings>=2.2.1

# ── Computer Vision ─────────────────────────────────────────
# NOTE: use opencv-python (not headless) on Windows for webcam support
opencv-python>=4.10.0
mediapipe>=0.10.30             # 0.10.30+ supports Python 3.13
Pillow>=10.3.0
imageio>=2.34.1

# ── Deep Learning ───────────────────────────────────────────
# PyTorch — CPU-only (works on all Windows machines, no CUDA needed)
# If you have an NVIDIA GPU, replace these 3 lines with the GPU install
# command from: https://pytorch.org/get-started/locally/
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0

timm>=1.0.3                    # Pretrained ViT / EfficientNet
ultralytics>=8.2.18            # YOLOv8 + YOLO-Pose (auto-downloads weights)
transformers>=4.41.0           # HuggingFace Transformers
einops>=0.8.0

# ── Anomaly Detection ───────────────────────────────────────
scikit-learn>=1.4.2
pyod>=1.1.3
statsmodels>=0.14.2

# ── LangChain / LangGraph Agent ─────────────────────────────
langchain>=0.2.1
langchain-community>=0.2.1
langchain-core>=0.2.1
langgraph>=0.1.1
langchain-openai>=0.1.8
langchain-ollama>=0.1.1

# ── RAG / Vector Database ───────────────────────────────────
# faiss-cpu — use the wheel below on Python 3.13 Windows if it fails:
#   pip install faiss-cpu --extra-index-url https://download.pytorch.org/whl/cpu
faiss-cpu>=1.8.0
chromadb>=0.5.0
sentence-transformers>=3.0.1
pypdf>=4.2.0

# ── Database ────────────────────────────────────────────────
sqlalchemy>=2.0.30
alembic>=1.13.1
asyncpg>=0.29.0
redis>=5.0.4

# ── Alerting / Notifications ────────────────────────────────
twilio>=9.1.0
sendgrid>=6.11.0

# ── GPS / Location ──────────────────────────────────────────
geopy>=2.4.1

# ── Monitoring / Logging ────────────────────────────────────
prometheus-client>=0.20.0
structlog>=24.2.0
mlflow>=2.13.0

# ── Serialisation / Utils ───────────────────────────────────
numpy>=1.26.4
pandas>=2.2.2
python-dotenv>=1.0.1
orjson>=3.10.3
tenacity>=8.3.0
APScheduler>=3.10.4
psutil>=5.9.0
